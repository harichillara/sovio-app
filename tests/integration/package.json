{
  "name": "@sovio/integration-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Live-Supabase integration tests. Opt-in via `pnpm test:integration`.",
  "scripts": {
    "test": "vitest run --config ./vitest.integration.config.ts",
    "test:watch": "vitest --config ./vitest.integration.config.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "typescript": "^5.4.0",
    "vitest": "^4.1.4"
  }
}
