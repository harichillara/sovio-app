interface:
  display_name: "Sovio App"
  short_description: "Repo-specific patterns and workflows for sovio-app"
  default_prompt: "Use the sovio-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true