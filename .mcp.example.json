{
  "_comment": "Template. Copy to .mcp.json and replace PASTE_YOUR_SUPABASE_PAT_HERE with a personal access token from https://supabase.com/dashboard/account/tokens. The real .mcp.json is gitignored. For CI/cloud, leave the value as ${SUPABASE_ACCESS_TOKEN} and inject the secret via the platform's env.",
  "mcpServers": {
    "supabase-sovio": {
      "command": "npx",
      "args": [
        "-y",
        "@supabase/mcp-server-supabase@latest",
        "--project-ref=kfqjapikievrgmszrimw",
        "--read-only"
      ],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "PASTE_YOUR_SUPABASE_PAT_HERE"
      }
    }
  }
}
